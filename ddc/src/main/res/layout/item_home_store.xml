<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <ImageView
        android:id="@+id/storePic"
        android:layout_width="120dp"
        android:layout_height="90dp"
        android:layout_marginEnd="10dp"
        android:layout_marginRight="15dp" />

    <TextView
        android:id="@+id/storeName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:layout_toEndOf="@id/storePic"
        android:layout_toRightOf="@id/storePic"
        android:text="店铺名称" />

    <RatingBar
        android:id="@+id/ratingBar"
        style="?android:attr/ratingBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/storeName"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@id/storePic"
        android:layout_toRightOf="@id/storePic"
        android:rating="4" />

    <TextView
        android:id="@+id/type"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/ratingBar"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@id/storePic"
        android:layout_toRightOf="@id/storePic"
        android:background="@drawable/home_type_border"
        android:padding="3dp"
        android:text="生活超市"
        android:textColor="@color/orange" />

    <TextView
        android:id="@+id/distance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@id/storeName"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:drawableLeft="@mipmap/home_store_locate"
        android:drawablePadding="5dp"
        android:drawableStart="@mipmap/home_store_locate"
        android:text="115m" />

</RelativeLayout>